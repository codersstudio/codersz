func main() {
    @console.log("JTL 파일 병합기 시작");

    @prompt `
        현재 디렉토리 *.jtl.gz 파일을 목록을 불러와서
        압축 해제를 하지 않고 "merged.jtl" 파일로 병합한다.
        메모리를 적게 사용하기 위해 스트리밍 방식으로 처리한다.
        첫번째 파일의 헤더는 유지하고 이후 파일들은 헤더를 제거한 후 병합하는
        코드를 작성한다.
    ` 
}